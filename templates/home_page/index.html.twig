{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="title">{{ title }}</h1>

        <div class="product-items grid">
            {% for i in 0..5 %}
                <figure class="product">
                    <img src="https://picsum.photos/250/250" alt="картинка">
                    <figcaption>
                        <p>Очень нужный товар</p>
                    </figcaption>
                </figure>
            {% endfor %}
        </div>

        <a class="link-product" href="{{path('product') }}">В каталог</a>
{#        #}
    </div>
{% endblock %}
